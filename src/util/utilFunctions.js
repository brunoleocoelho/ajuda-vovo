
/** Aumenta as fontes do APP */
export const increaseFonts = (realPercent) => {
    return realPercent + 0.2;
}

/** Reduz as dontes do APP */
export const decreaseFonts = (realPercent) => {
    return realPercent - 0.2;
}